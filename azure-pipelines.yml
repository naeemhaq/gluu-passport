# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
  displayName: 'npm install and build'

- task: DownloadPipelineArtifact@2
  inputs:
    buildType: 'specific'
    project: '32be4b0e-237f-430d-9289-c5966a5c9829'
    definition: '3'
    buildVersionToDownload: 'latest'
    artifactName: 'passport-saml'
    targetPath: '$(Pipeline.Workspace)/saml'
- task: DownloadPipelineArtifact@2
  inputs:
    buildType: 'specific'
    project: '32be4b0e-237f-430d-9289-c5966a5c9829'
    definition: '4'
    buildVersionToDownload: 'latest'
    artifactName: 'passport-openidconnect'
    targetPath: '$(Pipeline.Workspace)/openid'

- script: |
    pwd
    ls -al 
    npm update $(Pipeline.Workspace)/saml/s/
    echo '***** saml instal executed ****'
    npm update $(Pipeline.Workspace)/openid/s
    uname -a 
    npm -v
  displayName: 'Update packages'

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Pipeline.Workspace)'
    artifact: 'gluu-passport'
    publishLocation: 'pipeline'


